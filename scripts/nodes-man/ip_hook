(87..114).each do |i|
	ip_list += "172.16.0.#{i}\n"
end

=begin
(97..104).each do |i| 
        ip_list += "172.16.0.#{i}\n"
end

(107..114).each do |i| 
        ip_list += "172.16.0.#{i}\n"
end


#ip_list = IO.read("ips").chomp if File.exists?("ips")

#pp ip_list

=end

